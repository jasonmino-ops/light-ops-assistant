{
  "name": "e-shop-tray",
  "productName": "E-Shop Tray",
  "version": "0.1.0",
  "description": "E-Shop Tray V0.1 — LAN HTTP to the existing Windows printing path",
  "private": true,
  "main": "dist/main.js",
  "author": "E-Shop (店小二)",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "compile": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "check": "npm run typecheck && npm test && npm run compile",
    "start": "npm run compile && electron .",
    "pack:win": "npm run check && electron-builder --win --x64 --dir --publish never",
    "dist:win": "npm run check && electron-builder --win --x64 --publish never",
    "dist:win:field-sandbox": "npm run check && electron-builder --config electron-builder.field-sandbox.yml --win --x64 --publish never",
    "verify:field-sandbox": "node scripts/verify-field-sandbox-release.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^33.3.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.8.0",
    "vitest": "^2.1.9"
  }
}
