# Cashier-Offline-01 冻结记录

## 冻结结论

Cashier-Offline-01 已完成并冻结。`/cashier` 已具备 IndexedDB 商品缓存能力，并能显示在线/离线状态、商品缓存数量和缓存时间。断网时页面明确提示本阶段不支持离线收银，未开放离线订单保存或同步能力。

## 冻结 commit

`9e77435`

## 冻结内容

1. `/cashier` 联网加载商品后写入 IndexedDB。
2. 新增 `light_ops_cashier_offline` 本地缓存。
3. 显示当前在线/离线状态。
4. 显示已缓存商品数量和缓存时间。
5. 离线时提示“本阶段不支持离线收银”。
6. 新增 Offline-01 测试说明文档。
7. 更新离线收银设计文档中的阶段说明。

## 未改内容

1. 未改数据库。
2. 未新增 Prisma migration。
3. 未开放离线订单保存。
4. 未新增离线订单同步接口。
5. 未改 CASH / KHQR / records 主流程。
6. 未做离线 CASH 收银。

## 验收结果

1. 联网打开 `/cashier?storeCode=ST169E7000` 后商品缓存正常。
2. 页面显示在线状态。
3. 页面显示商品缓存数量和缓存时间。
4. 断网后页面显示离线状态和不支持离线收银提示。
5. 在线 CASH / KHQR 原收银流程未受影响。

## 风险

低。Offline-01 只建立商品缓存和状态提示基础，不产生离线订单，不影响销售记录和支付主链路。

## 后续建议

Offline-02 可进入“离线 CASH 订单本地保存”，但仍不得新增同步接口或修改数据库。
