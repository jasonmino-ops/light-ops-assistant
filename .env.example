# ── 数据库连接（必填）────────────────────────────────────────────────────────
# 本地开发：PostgreSQL 连接串
# Vercel 生产：在 Project Settings → Environment Variables 填写 Neon/Supabase 连接串
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?sslmode=require"

# ── 开发期角色切换（本地 dev 专用）───────────────────────────────────────────
# STAFF → 进入员工端（首页 /home，底部导航：销售/退款/记录）
# OWNER → 进入老板端（首页 /dashboard，底部导航：概览/记录）
DEV_ROLE=STAFF
