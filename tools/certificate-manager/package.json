{
  "name": "eshop-certificate-manager",
  "productName": "E-Shop Certificate Manager",
  "version": "0.1.1",
  "description": "E-Shop 自签名 Root Certificate 本机部署工具（QZ Tray 信任配置）",
  "private": true,
  "main": "dist/main/main.js",
  "author": "E-Shop (店小二)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "compile": "node scripts/clean-dist.mjs && tsc -p tsconfig.build.json && node scripts/copy-renderer.mjs",
    "start": "npm run compile && electron .",
    "test": "npm run compile && vitest run",
    "make:test-package": "node scripts/make-test-package.mjs",
    "pack:win": "npm run compile && electron-builder --win --x64 --publish never && node scripts/verify-portable-manifest.mjs",
    "verify:portable": "node scripts/verify-portable-manifest.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^33.3.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.8.0",
    "vitest": "^2.1.9"
  }
}
